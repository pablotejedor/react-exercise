import { Text } from '@chakra-ui/react';
import React from 'react';

const PageNotFound = () => {
  return <Text>La página a la que está intentando acceder no existe</Text>;
};

export default PageNotFound;
